{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter ./scripts exec tsx local/vercelEnvGuard.ts && pnpm run codegen && pnpm --filter @workspace/api-server build && pnpm --filter @workspace/trader-dashboard build",
  "outputDirectory": "artifacts/trader-dashboard/dist/public",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
